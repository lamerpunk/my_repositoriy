#include <iostream>
#include "utils.h"
#include <cmath>
#include "boolean.h"
#include "if.h"
#include "for.h"
#include "integer.h"


int main()
{ 

  std::cout << integer13(234)<<std::endl;



//   int x = 0;
//  // x == 0 ? (std::cout <<"ноль\n") : ("не ноль\n");
//   std::cout << (x == 0 ? "Ноль" : "Не ноль") <<std::endl;
  return 0;
}

